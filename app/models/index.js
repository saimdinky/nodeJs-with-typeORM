const { User } = require('./user.entity');
const { Role } = require('./role.entity');
const { Permission } = require('./permission.entity');
const { ClientToken } = require('./clientToken.entity');

module.exports = {
  User,
  Role,
  Permission,
  ClientToken,
};
